/*Validar que los tokens estan bien que, ya puede ser que no haya comandos validos, 
que el primer token sea un | o que solo haya un token con < > o que no haya ningun archivo arhchivo especificado
detras de > o de < */
#include "../../inc/headers/minishell.h"

int	validator_tony(t_list *tokens)
{

}